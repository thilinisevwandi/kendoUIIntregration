import { InjectionToken } from "@angular/core";

export const sovPanelHeaderListToken = new InjectionToken('sovPanelHeaderListToken');

export const sovPanelHeaderList = {
    headerName : ['Schedule Items', 'Previouse Sunmissions', 'This Submission', 'Submitted todate', 'Remaining','Retaining']
}